//! Shared REST API contract placeholders.

/// Marker for the REST contract namespace.
///
/// This type keeps the module intentional while the API surface is still empty.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct RestContracts;
