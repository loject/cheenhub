//! Shared media contract placeholders.

/// Marker for the WebTransport media contract namespace.
///
/// This type keeps the module intentional while the media protocol is still empty.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct MediaContracts;
